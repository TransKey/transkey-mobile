import React from 'react';
import {Root} from 'native-base';

import {AppNavigation} from './src/navigation';

const App = () => {
  return (
    <Root>
      <AppNavigation />
    </Root>
  );
};

export default App;
