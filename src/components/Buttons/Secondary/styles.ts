import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  button: {
    backgroundColor: '#7a0202',
    borderRadius: 5,
  },
  text: {
    color: '#ffffff',
    fontSize: 20,
  },
});
