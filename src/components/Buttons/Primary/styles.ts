import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  button: {
    backgroundColor: '#017003',
    borderRadius: 5,
  },
  text: {
    color: '#ffffff',
    fontSize: 20,
  },
});
